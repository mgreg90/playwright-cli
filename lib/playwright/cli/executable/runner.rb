# This class is for running the playwright commands from the command line,
# like `playwright generate`, `playwright get`, or `playwright publish`
module Playwright::Cli::Executable
  class Runner
  end
end